# Playtest session — 2026-04-06-bf355a4-r02

| Field | Value |
|--------|--------|
| **session_id** | `2026-04-06-bf355a4-r02` |
| **recorded_at** | `2026-04-06T05:36:00Z` |
| **commit** | `bf355a4224c8356cc641bf461e3478715b4551ae` (short: `bf355a4`) |
| **dev_url** | `http://127.0.0.1:5174/` |
| **browser_tool** | `cursor-ide-browser` |
| **agent / operator** | Cursor agent (follow-up: canvas focus) |
| **rubric** | v2 — **0–100** (100 = CoD / Far Cry bar); scores **recalibrated** from legacy 1–5 in `history.jsonl` |

## Summary

- **Change vs r01:** Applied **viewport screenshot → `browser_mouse_click_xy`** on the **left game area**; MCP reported target **`<canvas>`**. After that, **`.`** toggled dev overlay as expected; **W**, **U I J K** sent with canvas focused. **USER** observation confirmed: keys do nothing after reload until the window/canvas is clicked — now **documented** in `AGENT_RUNBOOK.md` and `/playtest`.
- **fun_60s_gate:** `true` — same rationale as r01; pause help (**Esc**) lists strikes; movement + four limb keys exercised after focus.
- **smoke_all_pass:** `false` — still no in-world **sign + Enter**, no **5 min** stress, no **true chord** hold, no **audio** verification, limited **ragdoll** stress.
- **blockers:** Narrowed vs r01: **Period + slider focus** issue mitigated by **canvas-first** workflow; remaining gaps are depth of smoke + automation limits.

## Scores (0–100, AAA-calibrated)

| Dimension | Score | Why this number |
|-----------|------:|-----------------|
| feel_striking | 24 | **+6 vs r01:** canvas focus makes input path **credible**; still **no** CoD-class hit read, juice audit, or latency proof — huge room to grow. |
| readability_combat | 24 | Same harsh bar as r01; focus does not fix silhouette/enemy read. |
| fatigue_keyboard | 48 | Bindings unchanged; slight bump acknowledging **intended** keyboard-first design, still not proven for long sessions. |
| camera_comfort | 28 | Marginally less distrust than r01 because gameplay loop was actually driven after focus; still thin evidence. |
| audio_clarity | 11 | Still **unverified** — must stay **low**. |
| stability_ragdoll_physics | 27 | Slightly above r01 only as “inputs actually reached sim”; no stress pass. |

## Scripted tasks — log

1. **Cold open:** Full **navigate** to `http://127.0.0.1:5174/`, wait ~2s.  
2. **Canvas focus:** Screenshot → **click (640, 360)** → `<canvas>`. **`.`** hide overlay attempt; **W** ×3, **u i j k**.  
3. **Pause:** **Esc** → help sections **TRAINING HALL** / **STRIKES**; **Resume** via button click (pause chrome only).  
4. **Re-focus:** Screenshot with dev drawer visible → **click (320, 380)** → `<canvas>`. **`.`** again.  
5. **Sign / 5 min / chord / audio:** Not completed this run.

## Evidence

- `browser_mouse_click_xy` mapped screenshot pixels to viewport and hit **`<canvas>`** (two clicks).  
- Prior session: `sessions/2026-04-05-bf355a4-r01.md`.

## Follow-ups (optional)

- Product: optional **`autofocus`** on canvas after mount to improve keyboard-first UX without a click (separate from playtest docs).
