# Playtest session — 2026-04-05-bf355a4-r02

| Field | Value |
|--------|--------|
| **session_id** | `2026-04-05-bf355a4-r02` |
| **recorded_at** | `2026-04-05T22:18:00Z` |
| **commit** | `bf355a4224c8356cc641bf461e3478715b4551ae` (short: `bf355a4`) |
| **dev_url** | `http://localhost:5173/?level=0` (also exercised bare `/`) |
| **browser_tool** | `cursor-ide-browser` |
| **agent / operator** | Cursor agent |
| **rubric** | `schema_version` **2** — scores **0–100** (100 = CoD / Far Cry bar per `RUBRIC.md`) |

## Summary

- **fun_60s_gate:** `true` — After canvas focus, **U / I / J / K** each fired a strike toward the **bag** from the default dojo layout without reading external docs (on-screen stamina + scene affordances).  
- **smoke_all_pass:** `false` — Failed/partial: **sign + Enter** not exercised at a kiosk; **simultaneous chord** not emitted by `browser_press_key` (`u+i` → single key); **per-limb SFX** not verifiable without audio; **~5 min** memory not measured; **ragdoll explosion** not observed in this short pass only (limited).  
- **blockers:** See `history.jsonl` line for this session (merged under **WS-223** / **WS-224** / **WS-226** per existing rows).

## Scores (0–100, brutal / AAA-calibrated)

| Dimension | Score | Why this number (required) |
|-----------|------:|----------------------------|
| feel_striking | 25 | Hit-stop / lunge read as prototype juice; contacts still feel light vs AAA weight; stick abstraction limits “impact closure.” |
| readability_combat | 25 | Silhouette + bag/dummy reads OK in sparse dojo; phase of strikes still mushy at a glance vs shipped action games. |
| fatigue_keyboard | 50 | **Inverted axis:** U I J K row is learnable and not finger-breaking; repeated tap strikes OK for a short agent session. |
| camera_comfort | 29 | Follow camera stable for lab space; minor “fighting the framerate polish” vs AAA third-person. |
| audio_clarity | 10 | **Not measured** in any meaningful mix sense — browser automation cannot attest to SFX distinctness; assume low until capture path exists. |
| stability_ragdoll_physics | 28 | No ragdoll “explosion” observed in limited dummy/bag spam; still early-articulated feel, not Far Cry-stable under stress. |

## Scripted tasks — log

1. **Cold open:** `npm run dev` already up → `http://localhost:5173/` interactive in ~2s after navigation (Vite client connected).  
2. **Reach bag / training target:** Keyboard **W** taps + spawn layout — **bag** visible and reachable in scene.  
3. **Three distinct moves:** **U**, **I**, **J**, **K** each pressed (four limb strikes).  
4. **Chord sample:** **`browser_press_key` `u+i`** only registered **I**; quick **U** → **I** chain attempted (~50ms gap) — chord not confirmed visually. **MCP limitation** for true holds.  
5. **Sign / help:** **Enter** away from kiosk did not surface sign modal in snapshot. **Esc** opened pause — help showed **WASD**, **U I J K**, **Shift** guard/dock, chord/chain copy aligned with `actionMap` / `dojoSignCopy`. **Second Escape** closed pause (toggle) — keyboard path without clicking **Resume**.  
6. **Stress (short):** Additional strike/movement bursts; **Rapier** deprecation error in console (see Evidence). No stuck-input observation.  
7. **Optional second level:** `http://localhost:5173/?level=0` loads same dojo index path cleanly.

## Evidence

- **Snapshots:** refs **e70–e85** (pause **Resume** / **Restart** / **Next**) when first **Esc** opened menu; help headings **TRAINING HALL**, **STRIKES AND DEFENSE** with live binding lines. Post-second-**Esc**, **Resume** controls absent (modal dismissed).  
- **Console:** Repeated **`using deprecated parameters for the initialization function; pass a single object instead`** from `@dimforge/rapier3d-compat` on load (`localhost:5173` and earlier `127.0.0.1:5174` traces in MCP log). Vite connect noise only otherwise.

## Follow-ups (optional)

- Document for agents: **Pause** can dismiss via **Esc** toggle (not only **Resume** click) — updates **WS-227** keyboard narrative.  
- **Chord / chain** automation: need true key-down overlap or human SOP (**WS-224**).
