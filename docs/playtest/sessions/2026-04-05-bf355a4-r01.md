# Playtest session — 2026-04-05-bf355a4-r01

| Field | Value |
|--------|--------|
| **session_id** | `2026-04-05-bf355a4-r01` |
| **recorded_at** | `2026-04-05T12:15:00Z` |
| **commit** | `bf355a4224c8356cc641bf461e3478715b4551ae` (short: `bf355a4`) |
| **dev_url** | `http://127.0.0.1:5174/` |
| **browser_tool** | `cursor-ide-browser` |
| **agent / operator** | Cursor agent (automated `/playtest`) |
| **rubric** | v2 — **0–100** (100 = CoD / Far Cry bar); scores **recalibrated** from legacy 1–5 in `history.jsonl` |

## Summary

- **fun_60s_gate:** `true` — After load, **Esc** opened pause; help text lists **U / I / J / K** strikes and chord/chain hints (matches `actionMap.ts`). **W** forward + **u, i, j, k** sent to the page within the first interaction window; screenshot showed playable dojo with bag and stamina UI.
- **smoke_all_pass:** `false` — Full QA smoke **not** completed: no **Enter** at an in-world **sign**, no **~5 min** stress / memory check, **simultaneous chord** not verified (MCP sends discrete key presses; only quick **u**→**i** chain attempted), **per-limb SFX** not audibly verified in automation, **ragdoll** not stress-tested.
- **blockers:** See JSONL + below.

## Scores (0–100, AAA-calibrated)

| Dimension | Score | Why this number |
|-----------|------:|-----------------|
| feel_striking | 18 | **Early prototype:** keys reached the page, but no verified hit confirmation, no defense vs CoD-tier responsiveness; dev overlay + automation limits. |
| readability_combat | 22 | Stick read at distance unproven; a11y tree is DOM sliders, not combat state; far from shipped action-title clarity. |
| fatigue_keyboard | 46 | **Bright spot only relatively:** **U I J K** + **W** are a sane laptop layout — still nowhere near “session-perfect” AAA port ergonomics proof. |
| camera_comfort | 26 | One screenshot slice; no combat stress; not validated against Far Cry–class follow. |
| audio_clarity | 11 | **Unverified in automation** — treat as **far below** commercial mix bar, not neutral. |
| stability_ragdoll_physics | 24 | Short run, no hard stress; “no explosion seen” ≠ good physics score under AAA expectations. |

## Scripted tasks — log

1. **Cold open:** Navigated to dev URL; ~1–2s to interactive (Vite + Rapier init). Console: recurring Rapier **deprecated init** message (library).
2. **Reach bag:** Spawn layout in screenshot already near bag; **W** pressed ×3 to move.
3. **Three distinct moves:** **u**, **i**, **j**, **k** pressed (four limbs).
4. **Chord:** True **U+I** hold not possible with discrete MCP presses; **u** then **i** after ~50ms (chain window per help **0.28s**) — outcome not confirmed in DOM.
5. **Sign / pause help:** **Esc** → pause modal with **TRAINING HALL** + **STRIKES AND DEFENSE** copy matching bindings; **Resume** via click (focus/Enter did not dismiss; mouse used **only** for pause chrome, not combat).
6. **Stress:** Short key burst only (~2–3 min **not** executed).
7. **Optional level:** Not run (`?level=` spot-check skipped).

## Evidence

- **Snapshots:** `browser_snapshot` on `viewId` **16735a** — dev **gameplay tuning** overlay dominates a11y tree (sliders/comboboxes). **Period** to hide overlay did not hide panel when focus likely on inputs (handler skips `HTMLInputElement`).
- **Console:** Vite connect logs; Rapier **“using deprecated parameters for the initialization function”** error (existing).

## Follow-ups (optional)

- Consider documenting **default dev URL port** (`5173` vs `5174`) in `AGENT_RUNBOOK.md` when multiple Vite instances run.
- Agent rubric: treat **smoke_all_pass** as **false** when audio, 5 min, and sign flows are skipped.
- **Period** vs focused range: blur focus to `body` before toggle, or exclude tuning sliders from tab order in dev (product decision).
