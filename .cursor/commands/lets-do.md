---
description: Task + @ roles from your text; ask when you need a decision
---

The user’s message (minus the slash command) is the **task**. If it’s empty, ask once what they want. Use the roles mentioned in the task. Ask questions from user when you need users input.

**Roles:** Every `@role-…`, `@.cursor/rules/role-….mdc`, or work-stream `` `role-…` `` → read `.cursor/rules/role-….mdc` and follow it.
