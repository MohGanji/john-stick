---
description: Run the user’s task; ask only when blocked on a decision
---

The user’s message (minus the slash command) is the **task**. If it’s empty, ask once what they want. Execute directly; ask questions only when you need a product or priority call you cannot infer.
